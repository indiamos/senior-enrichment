// This component isn't imported anywhere, because it's not required.
// But it seemed to me that there should be a student detail page.

import React from 'react';

export default function Student (props) {

  const student = props.student;
    console.log('store:', store);

  return (
    <section>
      <h2>Student</h2>
    </section>
  );
}
